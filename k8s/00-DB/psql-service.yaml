apiVersion: v1
kind: Service
metadata:
  name: psql-service
  namespace: "2023241041"
spec:
  selector:
    app: lidercom-db
  ports:
    - protocol: TCP
      port: 5432       # Puerto que usa el servicio
      targetPort: 5432 # Puerto que expone tu Pod
